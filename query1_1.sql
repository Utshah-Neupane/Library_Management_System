.mode table
.header on

ALTER TABLE BOOK_LOANS ADD COLUMN LATE INTEGER;

UPDATE BOOK_LOANS
SET LATE =
	CASE 
		WHEN (JULIANDAY(RETURNED_DATE) > JULIANDAY(DUE_DATE)) THEN 1
		WHEN RETURNED_DATE IS NULL THEN 1
		ELSE 0
END;








